<?xml version="1.0" encoding="utf-8"?>

<!DOCTYPE config [
    <!ELEMENT config ANY>

    <!ELEMENT server (name, listen+)>
    
    <!ELEMENT listen (allow*)>
    <!ATTLIST listen
        bind        CDATA   "0.0.0.0"
        port        CDATA   #REQUIRED
        ssl         CDATA   "disable"
        password    CDATA   #IMPLIED
    >

    <!ELEMENT allow EMPTY>
    <!ATTLIST allow
        mask        CDATA   #REQUIRED
        class       CDATA   #REQUIRED
        password    CDATA   #IMPLIED
        max         CDATA   #IMPLIED
    >

    <!ELEMENT operators (operator*)>
    
    <!ELEMENT operator EMPTY>
    <!ATTLIST operator
        mask        CDATA   #REQUIRED
        password    CDATA   #REQUIRED
        flags       CDATA   #REQUIRED
    >
]>

<config>
    <server>
        <name>Fail IRC</name>

        <motd><![CDATA[
            Welcome to a fail IRC.
        ]]></motd>
        
        <listen port="6667"/>
        <listen port="6697" ssl="enable"/>
    </server>

    <operators>
        <operator mask="meh!*@*" password="password" flags="netadmin"/>
    </operators>
</config>
